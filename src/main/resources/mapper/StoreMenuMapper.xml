<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="storeMenu">
	
	<select id="listStoreMenu" resultType="StoreMenuVO">
		SELECT	id
		,		group_id
		,		type_id
		, 		price
		,		name
		,		flag
		,		attribute_json
		,		description
		FROM	store.store_menu
		limit   #{page}, #{size}
	</select>
</mapper>