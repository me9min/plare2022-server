<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="AnswerMapper">
	
	<select id="listAnswer" resultType="AnswerVO">
		SELECT	board_answer_id
		,		member_id
		,		title
		,		content
		,		views
		,		date_of_created
		,		ip_of_created
		,		date_of_updated
		,		member_id_of_updated
		,		ip_of_updated
		,		status
		FROM	plare.board_answer
		where 	status = 'PUBLIC'
	</select>
	<select id="getAnswer" resultType="AnswerVO">
		SELECT	board_answer_id
		,		member_id
		,		title
		,		content
		,		views
		,		date_of_created
		,		ip_of_created
		,		date_of_updated
		,		member_id_of_updated
		,		ip_of_updated
		,		status
		FROM	plare.board_answer
		where 	status = 'PUBLIC'
		and 	board_answer_id=#{boardAnswerId}
	</select>
	<insert id="insertAnswer" >
		INSERT INTO plare.board_answer ( 
			member_id,		title,				content,				views,			date_of_created,
			ip_of_created,	date_of_updated,	member_id_of_updated,	ip_of_updated,	status
		) VALUES (
			#{memberId},	#{title},			#{content},				0,				now(),
			#{ipOfCreated},	now(),				#{memberIdOfUpdated},	#{ipOfUpdated},	'PUBLIC'
		);
	</insert>
	<update id="updateAnswer" >
		UPDATE	plare.board_answer
		SET		title=#{tittle}
		,		content=#{content}
		,		date_of_updated=NOW()
		,		member_id_of_updated=#{memberId}
		,		ip_of_updated=#{ipOfUpdated}	
		WHERE	board_answer_id=#{boardAnswerId};
		AND 	member_id=#{memberId}
		AND 	status='PUBLIC'
	</update>
	<update id="deleteAnswer" >
		UPDATE	plare.board_answer
		SET		status='PRIVATE'
		,		ip_of_updated=#{ipOfUpdated}	
		WHERE	board_answer_id=#{boardAnswerId}
		AND 	member_id=#{memberId}
		AND 	status='PUBLIC'
	</update>
	
</mapper>